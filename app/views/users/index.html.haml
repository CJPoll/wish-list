%h1 Listing users

%table
	%tr
		%th
		%th
		%th

	- @users.each do |user|
		%tr
			%td= link_to user.email, user
			%td= link_to 'Edit', edit_user_path(user)
			%td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }, id: "destroy_#{user.id}"

%br

= link_to 'New User', new_user_path
